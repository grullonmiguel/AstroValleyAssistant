<UserControl x:Class="AstroValleyAssistant.Views.RealAuctionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstroValleyAssistant.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Style="{StaticResource ViewSlideInFromTopStyle}" Margin="0 0 16 16" >

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel >
            <Border Background="{StaticResource Brush.Accent}" Padding="16 10">                
                <TextBlock Style="{StaticResource Theme.TextBlock.SidebarLabel}" Text="State" />
            </Border>
            <Border Padding="16" BorderBrush="{StaticResource Brush.Accent}" BorderThickness="1">
                <ComboBox />
            </Border>

            <Border Background="{StaticResource Brush.Accent}" Padding="16 10" Margin="0 16 0 0">
                <TextBlock Style="{StaticResource Theme.TextBlock.SidebarLabel}" Text="County" />
            </Border>
            <Border Padding="16" BorderBrush="{StaticResource Brush.Accent}" BorderThickness="1">
                <ComboBox />
            </Border>

            <Border Background="{StaticResource Brush.Accent}" Padding="16 10" Margin="0 16 0 0">
                <TextBlock Style="{StaticResource Theme.TextBlock.SidebarLabel}" Text="Date" />
            </Border>
            <Border Padding="16" BorderBrush="{StaticResource Brush.Accent}" BorderThickness="1">
                <ComboBox />
            </Border>
        </StackPanel>

        <Grid Grid.Column="1" Margin="16 0 0 0 ">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <Border Background="{StaticResource Brush.Accent}" Padding="16" Height="40">
                <Button HorizontalAlignment="Right" Content="Scrape" Command="{Binding ScrapeRealAuctionCommand}" />
            </Border>
        </Grid>
        
        <TextBlock Text="Real Auction View" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1" />
    </Grid>
</UserControl>
