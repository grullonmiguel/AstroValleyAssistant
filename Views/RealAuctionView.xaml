<UserControl x:Class="AstroValleyAssistant.Views.RealAuctionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstroValleyAssistant.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Style="{StaticResource ViewSlideInFromTopStyle}" Margin="0 0 16 16" >

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel >
            <Border Background="{StaticResource Brush.Accent}" Padding="16 10">                
                <TextBlock Style="{StaticResource Theme.TextBlock.SidebarLabel}" Text="State" />
            </Border>
            <Border Padding="16" BorderBrush="{StaticResource Brush.Accent}" BorderThickness="1">
                <ComboBox />
            </Border>

            <Border Background="{StaticResource Brush.Accent}" Padding="16 10" Margin="0 16 0 0">
                <TextBlock Style="{StaticResource Theme.TextBlock.SidebarLabel}" Text="County" />
            </Border>
            <Border Padding="16" BorderBrush="{StaticResource Brush.Accent}" BorderThickness="1">
                <ComboBox />
            </Border>

            <Border Background="{StaticResource Brush.Accent}" Padding="16 10" Margin="0 16 0 0">
                <TextBlock Style="{StaticResource Theme.TextBlock.SidebarLabel}" Text="Date" />
            </Border>
            <Border Padding="16" BorderBrush="{StaticResource Brush.Accent}" BorderThickness="1">
                <ComboBox />
            </Border>
        </StackPanel>

        <Grid Grid.Column="1" Margin="16 0 0 0 ">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>

            <Border Background="{StaticResource Brush.Accent}" Padding="16 10" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Style="{StaticResource Theme.TextBlock.SidebarLabel}" Text="{Binding Status}" />

                    <ProgressBar IsIndeterminate="True" Grid.Column="1" Width="180" HorizontalAlignment="Left"
                                 Height="10" 
                                 Visibility="{Binding IsScraping, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <Button Grid.Column="2" Content="Scrape" Command="{Binding ScrapeRealAuctionCommand}" />
                </Grid>
            </Border>

            <DataGrid ItemsSource="{Binding AuctionRecords}" Grid.Row="1"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Parcel ID" Binding="{Binding ParcelId}" Width="Auto"/>
                    <DataGridTextColumn Header="Address" Binding="{Binding Address}" Width="*"/>
                    <DataGridTemplateColumn Header="Appraiser" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center">
                                    <Hyperlink NavigateUri="{Binding AppraiserUrl}" RequestNavigate="Hyperlink_RequestNavigate">
                                        View
                                    </Hyperlink>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Assessed" Binding="{Binding AssessedValue, StringFormat={}{0:C}}" Width="100"/>
                    <DataGridTextColumn Header="Opening Bid" Binding="{Binding Bid, StringFormat={}{0:C}}" Width="100"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding DateDisplay, StringFormat=\{0:MM/dd/yyyy\}}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
