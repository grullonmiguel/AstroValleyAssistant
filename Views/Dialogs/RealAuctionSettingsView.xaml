<UserControl x:Class="AstroValleyAssistant.Views.Dialogs.RealAuctionSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AstroValleyAssistant.Views.Dialogs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <TextBlock Text="Real Auction" FontSize="24" Foreground="{DynamicResource Brush.Accent}"/>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" FontSize="16" Margin="0 8" Text="Choose State, County and Auction Date" />

        <!-- State -->
        <TextBlock Text="State" FontWeight="Bold" Grid.Row="2"/>
        <ComboBox
            Grid.Row="3"
            Margin="0 8 0 0"
            Background="Transparent"
            ItemsSource="{Binding States}"
            SelectedItem="{Binding Path=StateSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
