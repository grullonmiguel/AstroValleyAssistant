<UserControl x:Class="AstroValleyAssistant.Views.Dialogs.CountyMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:AstroValleyAssistant.Core.Behaviors"
             xmlns:converters="clr-namespace:AstroValleyAssistant.Core.Converters"
             xmlns:local="clr-namespace:AstroValleyAssistant.Views.Dialogs"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" >
    <UserControl.Resources>
        <converters:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter"/>
        <converters:CenterPointConverter x:Key="CenterPointConverter"/>
    </UserControl.Resources>
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListBox Grid.Column="0"
                 Style="{StaticResource Theme.ListBox.BaseMapList}"
                 ItemsSource="{Binding Counties}"
                 SelectedItem="{Binding SelectedCounty, Mode=TwoWay}"
                 behaviors:ListBoxSmartScrollBehavior.ScrollToSelectedItem="{Binding SelectedCounty}">
        </ListBox>

        <Grid Grid.Column="1">
            <Viewbox>
                <ItemsControl ItemsSource="{Binding Counties}" Width="{Binding MapWidth}" Height="{Binding MapHeight}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Canvas>
                                <Path Data="{Binding PathData}" Style="{StaticResource Theme.Path.MapElement}">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="MouseEnter">
                                            <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="IsHovered" Value="True"/>
                                        </b:EventTrigger>
                                        <b:EventTrigger EventName="MouseLeave">
                                            <b:ChangePropertyAction TargetObject="{Binding}" PropertyName="IsHovered" Value="False"/>
                                        </b:EventTrigger>
                                        <b:EventTrigger EventName="MouseDown">
                                            <b:ChangePropertyAction TargetObject="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ItemsControl}}" PropertyName="SelectedCounty" Value="{Binding}"/>
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </Path>

                                <TextBlock Text="{Binding Name}" Style="{StaticResource Theme.TextBlock.MapLabel}">                            
                                    <Canvas.Left>
                                        <MultiBinding Converter="{StaticResource CenterPointConverter}" ConverterParameter="X">
                                            <Binding Path="PathData.Bounds"/>
                                            <Binding Path="ActualWidth" RelativeSource="{RelativeSource Self}"/>
                                        </MultiBinding>
                                    </Canvas.Left>
                                    <Canvas.Top>
                                        <MultiBinding Converter="{StaticResource CenterPointConverter}" ConverterParameter="Y">
                                            <Binding Path="PathData.Bounds"/>
                                            <Binding Path="ActualHeight" RelativeSource="{RelativeSource Self}"/>
                                        </MultiBinding>
                                    </Canvas.Top>
                                </TextBlock>
                            </Canvas>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Viewbox>
        </Grid>
    </Grid>
</UserControl>