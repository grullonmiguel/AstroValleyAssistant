<UserControl x:Class="AstroValley.Presentation.Views.MultipleMatchesBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <!--
        USAGE:
        
        Drop this control wherever the banner should appear.
        Bind the four dependency properties to your ViewModel:

        <controls:MultipleMatchesBanner
            MatchCount="{Binding PropertySelected.Matches.Count, Mode=OneWay}"
            ParcelId="{Binding PropertySelected.ParcelId, Mode=OneWay}"
            ViewMatchesCommand="{Binding ViewMatchesCommand}"
            ViewMatchesCommandParameter="{Binding PropertySelected}" />
    -->
    
    <Border Style="{StaticResource MultipleMatchesBanner.Border}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Info Icon -->
            <Path Grid.Column="0"
                  Data="{StaticResource Icon.Info}"
                  Style="{StaticResource MultipleMatchesBanner.Icon}"/>

            <!-- Message Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource MultipleMatchesBanner.Title}">
                    <Run Text="{Binding MatchCount, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"/>
                    <Run Text="matches found for parcel"/>
                    <Run Text="{Binding ParcelId, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Text="Select the correct property to resolve this match." Style="{StaticResource MultipleMatchesBanner.Subtitle}"/>
            </StackPanel>

            <!-- View Matches Button -->
            <Button Grid.Column="2"
                    Content="View Matches"
                    Style="{StaticResource MultipleMatchesBanner.Button}"
                    Command="{Binding ViewMatchesCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding ViewMatchesCommandParameter, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid>
    </Border>
</UserControl>
