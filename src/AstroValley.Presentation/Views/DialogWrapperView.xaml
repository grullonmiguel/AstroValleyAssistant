<UserControl x:Class="AstroValley.Presentation.Views.DialogWrapperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600">
    <DockPanel>
        <!-- Title Bar -->
        <Grid DockPanel.Dock="Top" Height="50" Background="Transparent">
            <TextBlock 
                VerticalAlignment="Center" 
                Margin="16,0,0,0" 
                FontSize="20" 
                LineHeight="24" 
                Foreground="{DynamicResource Brush.Accent}"
                Text="{Binding Title}"/>

            <Button 
                Content="✕" 
                HorizontalAlignment="Right" 
                Style="{DynamicResource Theme.Button.IconOnly}"
                Padding="15,0"
                FontSize="16"
                Command="{Binding CloseCommand}"/>
        </Grid>

        <!-- Content -->
        <ContentPresenter Content="{Binding ContentViewModel}" Margin="12"/>
    </DockPanel>
</UserControl>
